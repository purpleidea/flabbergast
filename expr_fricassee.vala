namespace Flabbergast.Expressions {
	/*
   "for" ("ordinal"? attr:name "=")? value0:name ("," valuen:name)* "in" input0:expr ("," inputn:expr) ("where" where:expr)? ("order" "by" order:expr)? ("select" (select_attr:expr "=")? select_value:expr) | "reduce" reduce:expr "with" initial_name:name "=" initial_value:expr)
 */
}
